# Olve.Logging [DEPRECATED]

> **This package is deprecated.** Use `Microsoft.Extensions.Logging` or another established logging framework instead. This package will not receive further updates.

A lightweight in-memory logging library previously used across the Olve.* libraries.
